# ─────────────────────────────────────────────
#  VoiceBridge - Requirements
# ─────────────────────────────────────────────

# Web Framework & Serverless API
fastapi>=0.110.0
uvicorn>=0.30.0
python-multipart>=0.0.9
requests>=2.31.0

# Translation & Language Detection
deep-translator>=1.11.0
langdetect>=1.0.9

# Neural Text-to-Speech & Speech Recognition
gTTS>=2.5.0
SpeechRecognition>=3.10.0

# Optional Local Mic & Desktop packages (Windows only)
sounddevice>=0.5.0; sys_platform == 'win32'
pyttsx3>=2.90; sys_platform == 'win32'
